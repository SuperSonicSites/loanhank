# Copy to .dev.vars for local `pnpm dev`. In production every one of these is a
# Worker secret set with `wrangler secret put NAME`. Never commit real values.
#
# NOT in this file: CLOUDFLARE_API_TOKEN / CLOUDFLARE_ACCOUNT_ID are CI-side
# secrets (GitHub Actions), never Worker secrets. D1/R2 bindings live in
# wrangler.jsonc, not env.

# Vision extraction provider (spec.md Decision 5). Set a hard monthly spend cap
# on the provider account itself, not only in code.
OPENAI_API_KEY=
# Optional override of the extraction model id; default lives in src/api/extractor.ts.
EXTRACTION_MODEL=

# Cloudflare Turnstile, invisible mode on submit (spec.md Decision 7).
# Prefilled with Cloudflare's documented always-pass TEST keypair so local dev
# works out of the box. Real keys: dashboard → Turnstile. The real site key is
# public and ships in wrangler.jsonc vars; only the secret key is a secret.
# Full dummy-key set (always-fail, token-spent): Cloudflare Turnstile testing docs.
TURNSTILE_SITE_KEY=1x00000000000000000000AA
TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA

# Transactional email for the teardown PDF (spec.md Decisions 6 and 10).
# Sender domain is mail.loanhank.com; SPF/DKIM/DMARC must be live before real sends.
RESEND_API_KEY=
EMAIL_FROM=hank@mail.loanhank.com

# Meta Conversions API, server-side only, no pixel (spec.md Decisions 8, 8a).
# The dataset id (what Meta used to call a pixel id) is public; the token is a
# Worker secret in production. An unset META_DATASET_ID switches the sender
# off entirely. LOCAL RULE: this must point at a DEV dataset, never the
# production one — Meta does not discard test_event_code traffic, so a test
# against the production dataset is a test row in the pile (spec.md §7.2).
#
# There is no DEV_ prefixed variant of these on purpose. This file is only ever
# read by `wrangler dev`; production reads Worker secrets set with
# `wrangler secret put`, and never reads this file at all. So the values here
# are the dev values by definition, and a second prefixed set would mean code
# choosing between them at runtime, which is one more way to point a live
# worker at the wrong dataset. One name, one meaning, and the environment
# decides which value it holds.
#
# The token is scoped to its dataset. A token generated on the production
# dataset cannot post to the dev one: Meta answers GraphMethodException code
# 100, subcode 33. Generate the token from within the dataset it is for
# (Events Manager, the dataset, Settings, Conversions API, generate).
META_DATASET_ID=
META_CAPI_TOKEN=
META_TEST_EVENT_CODE=


# Salt for hashing IPs in the per-extract rate limiter. Any long random string.
RATE_LIMIT_SALT=
