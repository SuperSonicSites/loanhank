{
  "name": "loanhank",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:eval": "vitest run tests/extraction-fixtures.test.ts tests/quote-eval.test.ts tests/photo-path.test.ts",
    "test:eval:live": "vitest run --config vitest.live.config.ts",
    "typecheck": "tsc --noEmit",
    "deploy": "node ops/deploy.mjs",
    "infra:r2": "wrangler r2 bucket lifecycle set loanhank-quotes --file infra/r2-lifecycle-quotes.json && wrangler r2 bucket lifecycle set loanhank-backups --file infra/r2-lifecycle-backups.json && wrangler r2 bucket lifecycle set loanhank-snapshots --file infra/r2-lifecycle-snapshots.json && pnpm infra:verify",
    "infra:verify": "wrangler r2 bucket lifecycle list loanhank-quotes && wrangler r2 bucket lifecycle list loanhank-backups && wrangler r2 bucket lifecycle list loanhank-snapshots",
    "funnel": "node ops/funnel.mjs",
    "corrections": "node ops/funnel.mjs corrections",
    "restore:drill": "node ops/restore-drill.mjs"
  },
  "dependencies": {
    "decimal.js": "10.6.0",
    "hono": "4.13.5",
    "openai": "7.10.0",
    "pdf-lib": "1.17.1",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260904.1",
    "@fontsource/courier-prime": "^5.3.0",
    "@fontsource/libre-franklin": "^5.3.0",
    "@types/node": "26.4.1",
    "typescript": "7.0.2",
    "vitest": "5.0.0",
    "wrangler": "4.129.0"
  }
}
